<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>Thông tin đăng ký khóa học</title>
  <style>
    body {
      font-family: "Arial", sans-serif;
      margin: 0 auto;
      padding: 20px;
      max-width: 800px;
      color: #333;
    }
    h2 {
      text-align: center;
      text-transform: uppercase;
      margin-bottom: 30px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 30px;
    }
    th, td {
      border: 1px solid #aaa;
      padding: 8px 12px;
      text-align: left;
    }
    th {
      background-color: #e6f2ff;
    }
    .info-table td {
      border: none;
      padding: 4px 8px;
    }
    .highlight {
      font-weight: bold;
    }
    .summary td {
      font-weight: bold;
    }
    .summary .total-row {
      background-color: #fef4d1;
    }
    .summary .discount-row {
      background-color: #fff8e1;
    }
    .summary .final-row {
      background-color: #e0f7e9;
    }
  </style>
</head>
<body>
  <h2>THÔNG TIN ĐĂNG KÝ KHÓA HỌC</h2>

  <table class="info-table">
    <tr>
      <td><strong>Tên Thánh:</strong> {{saint_name}}</td>
      <td><strong>Họ và Tên:</strong> {{full_name}}</td>
    </tr>
    <tr>
      <td><strong>Ngày sinh:</strong> {{birth_date}}</td>
      <td><strong>Nơi sinh:</strong> {{birth_place}}</td>
    </tr>
    <tr>
      <td><strong>Điện thoại:</strong> {{phone}}</td>
      <td><strong>Email:</strong> {{email}}</td>
    </tr>
    <tr>
      <td><strong>Giáo xứ/Dòng tu:</strong> {{parish}}</td>
      <td><strong>Địa chỉ:</strong> {{address}}</td>
    </tr>
  </table>

  <h3>THÔNG TIN ĐĂNG KÝ KHÓA HỌC:</h3>
  <table>
    <thead>
      <tr>
        <th>Stt</th>
        <th>Môn học</th>
        <th>Lịch học</th>
        <th>Học phí</th>
      </tr>
    </thead>
    <tbody>
      {{#each classes}}
        <tr>
          <td>{{this.index}}</td>
          <td>{{this.name}}</td>
          <td>{{this.schedule}}</td>
          <td>{{this.price}} đ</td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <table class="summary">
    <tr class="total-row">
      <td colspan="3">Tổng học phí:</td>
      <td>{{total_fee}} đ</td>
    </tr>
    <tr class="discount-row">
      <td colspan="3">Tiền đặt cọc:</td>
      <td>{{prepaid}} đ</td>
    </tr>
    <tr class="final-row">
      <td colspan="3">Nợ học phí:</td>
      <td>{{debt}} đ</td>
    </tr>
  </table>

  <p><strong>Hình thức thanh toán:</strong> {{payment_method}}</p>
</body>
</html>
